#ifndef H_PROG2

#define H_PROG2

#include<iostream> // Library to access input and output operations

#include<string> // Library to access strings in the program

using namespace std;

bool isPerfect(const unsigned int n); // We are function prototyping here as in we are declaring the function in the header file which has it definition in prog2.cc

void divisors(const unsigned int divisor);

const unsigned int finalValue = 9999; // As per the instruction provided in assignment I have defined the constant value in the header file.

#endif
